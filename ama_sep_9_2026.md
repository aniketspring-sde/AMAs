
## What is git status?
- git status is a Git command that shows the current state of your working directory and staging area.

## What is DIP in SOLID?
- DIP in SOLID stands for Dependency Inversion Principle.
- High-level modules should not depend directly on low-level modules. Both should depend on abstractions.

## What does the D in ACID stand for?
- In ACID, the D stands for Durability.
- Once a transaction has been successfully committed, its changes are permanently saved, even if the database crashes immediately afterward.

## How do you create a virtual environment in Python?
- python3 -m venv .venv
- source .venv/bin/activate
##  What are some string methods in Python?
- .upper()       
- .lower()       
- .title()       
- .capitalize() 
- .swapcase()


  ## What are triggers in SQL?
  - A trigger is a piece of code that automatically executes when a specified event happens in a database table.
  ## Why do we use the for attribute inside the <label> tag?
  - The for attribute in an HTML <label> connects the label to a specific form control.

  ## What is type conversion in Python?
  - Type conversion in Python means changing a value from one data type to another.
  ##  What is inheritance?
  - Inheritance is an Object-Oriented Programming (OOP) concept where one class inherits properties and behaviors from another class.
  ## Explain the different types of inheritance.
  - Single Inheritance: One child class inherits from one parent class.

  - Multilevel Inheritance: A class inherits from a child class, forming a chain.

  - Hierarchical Inheritance: Multiple child classes inherit from the same parent class.

  - Multiple Inheritance: One child class inherits from multiple parent classes.

  - Hybrid Inheritance: A combination of two or more types of inheritance.

  ## Explain CSS selectors.
  - Element selector: Selects all HTML elements of a specific tag.
  - Class selector: Selects elements with a specific class.
  - ID selector: Selects an element with a specific ID.
  - Universal selector: Selects all HTML elements.
  - Grouping selector: Selects multiple elements together.
  - Descendant selector: Selects elements inside another element.
  - Child selector: Selects direct children of an element.
  - Attribute selector: Selects elements based on attributes.
  - Pseudo-class selector: Selects elements based on their state.
  - Pseudo-element selector: Styles a specific part of an element.

  ## What is the symbol used for an ID selector in CSS?
  - \#
  ## Give some examples of relational databases.
  - MySQL
  - PostgreSQL
  - Oracle Database
  - Microsoft SQL Server
