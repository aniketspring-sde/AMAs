## Does JavaScript support multiple inheritance?
- No
## What is the find command in CLI?
- The find command in CLI is used to search for files and directories based on conditions like name, type, size, or modification time.

## What is a data type?
- A data type defines what kind of value a variable can store.
## What is the full form of DOM?
- Document Object Model.
## How do you check the folder structure in CLI?
- tree
## What is git init?
- git init initializes a new Git repository in the current directory
## What is the difference between primitive and non-primitive data types?
- Primitive data types hold simple values, while non-primitive data types hold references to more complex data such as objects and arrays.
## How do you find a group in CLI?
- id -gn
## Why do we use inheritance?
- To reduce code duplication. 
## What is a database?
- A database is an organized collection of data that can be stored, managed, retrieved, and updated efficiently.
## How do you rename files in CLI?
- using the mv (move) command.
## What is the full form of NaN?
- Not a Number
##  What structure does the DOM represent?
- The DOM represents an HTML document as a tree-like hierarchical structure of nodes.
